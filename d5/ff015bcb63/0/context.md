# Session Context

## User Prompts

### Prompt 1

Implement the following plan:

# Plan: `create-issue` 스킬 + GitHub 이슈 템플릿 생성

## Context

현재 프로젝트에는 `commit`과 `create-pr` 스킬만 존재하며, GitHub 이슈 생성을 위한 스킬이 없다. 또한 `.github/ISSUE_TEMPLATE/` 디렉토리도 없어서 이슈 템플릿이 미비한 상태다. 이 작업에서는:

1. **GitHub 이슈 템플릿**: `.github/ISSUE_TEMPLATE/`에 YAML form 기반 템플릿 생성 (best practices 적용)
2. **`create-issue` 스킬**:...

